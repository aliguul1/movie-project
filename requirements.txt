How to use these files together
Here is how to get it running from scratch:

Initialize the environment:

Verify the _static content: Make sure your index_template.html is inside _static. Without it, option 9 will show a "File Not Found" error.

Launch the App: